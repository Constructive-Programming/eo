package dev.constructive.eo.jsoniter

/** JSON-bytes → Avro-bytes direction of the jsoniter bridge (`JsoniterAvro` and the `.avro` face on
  * `JsoniterPrism` / `JsoniterTraversal`) — the mirror of `dev.constructive.eo.avro.jsoniter`.
  * cats-eo-avro is an '''Optional''' dependency of `cats-eo-jsoniter` — add it to use this package.
  */
package object avro
