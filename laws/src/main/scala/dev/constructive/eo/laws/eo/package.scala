package dev.constructive.eo.laws

/** Laws specific to eo's composition machinery rather than any single optic: compose
  * associativity/identity (`ComposeLaws`), fold/traverse consistency, `modifyA` coherence,
  * reverse/transform round-trips, and `Morph` faithfulness.
  */
package object eo
